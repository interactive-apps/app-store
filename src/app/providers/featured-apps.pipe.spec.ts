import { FeaturedAppsPipe } from './featured-apps.pipe';

describe('FeaturedAppsPipe', () => {
  it('create an instance', () => {
    const pipe = new FeaturedAppsPipe();
    expect(pipe).toBeTruthy();
  });
});
