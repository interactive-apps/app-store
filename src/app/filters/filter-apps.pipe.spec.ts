import { FilterAppsPipe } from './filter-apps.pipe';

describe('FilterAppsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAppsPipe();
    expect(pipe).toBeTruthy();
  });
});
